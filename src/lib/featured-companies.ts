// Hand-picked companies shown on /dashboard when no ticker is selected.
// Swap this for live "most searched" data once usage is high enough.
export const FEATURED_TICKERS = ['TSLA', 'MSFT', 'GOOGL', 'NVDA', 'META'] as const;
